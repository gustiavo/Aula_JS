const um = 1
const dois = 2
const tres = 3
const quatro = 4
const cinco = 5
const seis = 6
const sete = 7
const oito = 8
const nove = 9
const dez = 10
var tabuada = 1
var resultado = +1

do{
    var resultado = um * tabuada
    console.log(` ${um} x ${tabuada} = ${resultado} `)
    tabuada++


    do{
        var resultado = dois * tabuada
        console.log(` ${dois} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)
    

    do{
        var resultado = tres * tabuada
        console.log(` ${tres} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)

    do{
        var resultado = quatro * tabuada
        console.log(` ${quatro} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)

    do{
        var resultado = cinco * tabuada
        console.log(` ${cinco} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)

    do{
        var resultado = seis * tabuada
        console.log(` ${seis} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)
    
    do{
        var resultado = sete * tabuada
        console.log(` ${sete} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)
    
    do{
        var resultado = oito * tabuada
        console.log(` ${oito} x ${tabuada} = ${resultado} `)
        tabuada++
        
    }while( tabuada <= 10)

    
}while( tabuada <= 10)

do{
    var resultado = nove * tabuada
    console.log(` ${nove} x ${tabuada} = ${resultado} `)
    tabuada++
    
}while( tabuada <= 10)

do{
    var resultado = dez * tabuada
    console.log(` ${dez} x ${tabuada} = ${resultado} `)
    tabuada++
    
}while( tabuada <= 10)
